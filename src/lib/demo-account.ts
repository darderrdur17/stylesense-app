/**
 * Seeded demo user — run `npm run db:seed` after `db:push`.
 * For testing only; rotate or remove on a public production site.
 */
export const DEMO_USER_EMAIL = "demo@stylesense.app";
export const DEMO_USER_PASSWORD = "StyleSenseDemo!";
